#The each iterator '.each' returns all the elements of an array or a hash.
#Syntax
collection.each do |variable|
   code
end
#When run/called, it executes code for each element in collection. Here, collection could be an array or a ruby hash.